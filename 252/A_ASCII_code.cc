#include <bits/stdc++.h>
using namespace std;

// 整数で読んでchar型で出力する
int main() {
  int N;
  cin >> N;

  cout << (char)N << endl;

  // another answer
  // printfで表示してもよい
  // int n;
  // scanf("%d", &n);
  // printf("%c\n", n);
  return 0;
}
